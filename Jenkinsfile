pipeline {
    agent any

    stages {
        stage('Preparation') {
            steps {
                echo 'Jenkins pipeline connected successfully!'
                sh 'ls -la'
            }
        }
    }
}
